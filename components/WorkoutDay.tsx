import {Text, View, StyleSheet, Route} from 'react-native';
import {NavigationProp} from '@react-navigation/native';

interface Props
{
        exercise: string;
        navigation: NavigationProp<any>;
        route: Route;
}

function WorkoutDay()
{
        return (
                <>
                </>
        );
}

export default WorkoutDay;